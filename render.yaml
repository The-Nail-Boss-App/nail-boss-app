services:
  - type: web
    name: the-nail-boss
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    preDeployCommand: npm run db:migrate
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
