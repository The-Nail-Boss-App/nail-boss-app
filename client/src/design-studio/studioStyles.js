import { COLORS, S } from "../styles.js";

const ANITASET_MARBLE_BACKGROUND = "linear-gradient(135deg, rgba(255,255,255,.92), rgba(250,247,252,.88)), radial-gradient(circle at 18% 12%, rgba(255,255,255,.95), transparent 24%), radial-gradient(circle at 78% 18%, rgba(123,47,89,.08), transparent 28%), repeating-linear-gradient(118deg, rgba(120,111,120,.10) 0 1px, transparent 1px 34px), repeating-linear-gradient(36deg, rgba(255,255,255,.44) 0 1px, transparent 1px 46px)";

const LUXURY_TRANSITION = "transform 180ms ease, box-shadow 180ms ease, border-color 180ms ease, background 180ms ease, color 180ms ease, opacity 180ms ease";

// Artist Toolkit™ popovers share a fixed high z-index layer so quick controls are never clipped by the workspace.
// commandPolishPopover and commandFrenchTipPopover keep quick controls away from the hero canvas.
export const UI = {
  shell: {
    height: "100%",
    minHeight: 0,
    display: "flex",
    flexDirection: "column",
    background: ANITASET_MARBLE_BACKGROUND,
  },
  toolbar: {
    display: "flex",
    alignItems: "center",
    gap: 6,
    padding: "12px 18px",
    borderBottom: `1px solid ${COLORS.border}`,
    background: COLORS.surface,
    flexWrap: "nowrap",
  },
  artistCommandBar: {
    display: "grid",
    gridTemplateColumns:
      "minmax(0, .8fr) minmax(150px, .46fr) minmax(260px, .95fr)",
    width: "100%",
    maxWidth: "100%",
    boxSizing: "border-box",
    overflowX: "clip",
    gap: 6,
    alignItems: "center",
    padding: "4px clamp(8px, 1vw, 12px)",
    borderBottom: "1px solid rgba(123,47,89,.14)",
    background: "linear-gradient(135deg, #fffaf7 0%, #fff 48%, #fff4fb 100%)",
    boxShadow: "0 12px 32px rgba(60,20,50,.07)",
    flexWrap: "wrap",
  },
  commandIdentity: { minWidth: 0, maxWidth: "100%" },
  commandLogo: {
    fontFamily: "Georgia, serif",
    fontSize: 18,
    letterSpacing: ".16em",
    textTransform: "uppercase",
    color: COLORS.plum,
    fontWeight: 800,
    marginBottom: 3,
  },
  commandLogoImage: {
    width: 72,
    maxWidth: "48vw",
    height: "auto",
    display: "block",
    marginBottom: 3,
  },
  commandDesignName: {
    width: "100%",
    maxWidth: "100%",
    border: 0,
    outline: 0,
    background: "transparent",
    color: COLORS.text,
    fontFamily: "Georgia, serif",
    fontSize: "clamp(15px, 1.9vw, 22px)",
    lineHeight: 1,
    fontWeight: 700,
    padding: "2px 0",
    minWidth: 0,
    whiteSpace: "normal",
    overflow: "visible",
    overflowWrap: "anywhere",
    wordBreak: "break-word",
    textOverflow: "clip",
  },
  commandDesignNameCounter: {
    marginTop: 1,
    color: COLORS.textMuted,
    fontSize: 12,
    fontWeight: 800,
    letterSpacing: ".08em",
    textTransform: "uppercase",
    textAlign: "right",
  },
  commandCollection: {
    marginTop: 2,
    color: COLORS.textMuted,
    fontSize: 11,
    letterSpacing: ".16em",
    textTransform: "uppercase",
    fontWeight: 800,
  },
  commandCenter: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    gap: 7,
    minWidth: 0,
    flexWrap: "wrap",
  },
  autoSaveBadge: {
    border: "1px solid rgba(123,47,89,.14)",
    background: "rgba(255,255,255,.78)",
    borderRadius: 999,
    padding: "4px 7px",
    fontSize: 11,
    fontWeight: 900,
    letterSpacing: ".08em",
    textTransform: "uppercase",
    boxShadow: "0 10px 24px rgba(60,20,50,.07)",
    transition: LUXURY_TRANSITION,
  },
  currentPolishButton: {
    display: "flex",
    alignItems: "center",
    gap: 7,
    minHeight: 34,
    border: "1px solid rgba(123,47,89,.12)",
    background: "#fff",
    color: COLORS.text,
    borderRadius: 14,
    padding: "4px 7px",
    cursor: "pointer",
    boxShadow: "0 14px 34px rgba(60,20,50,.1)",
    transition: LUXURY_TRANSITION,
  },
  currentPolishText: {
    display: "flex",
    flexDirection: "column",
    gap: 2,
    fontSize: 11,
    fontWeight: 900,
    letterSpacing: ".06em",
    textTransform: "uppercase",
    textAlign: "left",
  },
  currentPolishMeta: {
    color: COLORS.textMuted,
    fontSize: 11,
    letterSpacing: ".02em",
    textTransform: "none",
  },
  commandActions: {
    display: "flex",
    justifyContent: "flex-end",
    alignItems: "center",
    gap: 6,
    flexWrap: "nowrap",
  },
  commandGroup: {
    position: "relative",
    display: "inline-flex",
    alignItems: "center",
  },
  commandPopover: {
    position: "fixed",
    top: 132,
    right: 18,
    zIndex: 1200,
    width: "min(320px, calc(100vw - 36px))",
    maxHeight: "calc(100vh - 154px)",
    overflow: "auto",
    display: "grid",
    gap: 7,
    padding: 10,
    border: "1px solid rgba(123,47,89,.12)",
    borderRadius: 14,
    background: "rgba(255,255,255,.98)",
    boxShadow: "0 24px 58px rgba(60,20,50,.18)",
  },
  commandPopoverWide: {
    position: "fixed",
    top: 132,
    right: 18,
    zIndex: 1200,
    width: "min(380px, calc(100vw - 36px))",
    maxHeight: "calc(100vh - 154px)",
    overflow: "auto",
    padding: 10,
    border: "1px solid rgba(123,47,89,.12)",
    borderRadius: 17,
    background: "rgba(255,255,255,.98)",
    boxShadow: "0 24px 58px rgba(60,20,50,.18)",
  },
  commandFrenchTipPopover: { position: "fixed", top: 132, left: 18,
    zIndex: 1200,
    width: "min(380px, calc(100vw - 36px))",
    maxHeight: "calc(100vh - 154px)",
    overflow: "auto",
    padding: 10,
    border: "1px solid rgba(123,47,89,.12)",
    borderRadius: 17,
    background: "rgba(255,255,255,.98)",
    boxShadow: "0 24px 58px rgba(60,20,50,.18)",
  },
  commandPolishPopover: { position: "fixed", top: 132, left: 18,
    zIndex: 1200,
    width: "min(360px, calc(100vw - 36px))",
    maxHeight: "calc(100vh - 154px)",
    overflow: "auto",
    padding: 10,
    border: "1px solid rgba(123,47,89,.12)",
    borderRadius: 17,
    background: "rgba(255,255,255,.98)",
    boxShadow: "0 24px 58px rgba(60,20,50,.18)",
  },
  commandButton: (active = false, disabled = false) => ({
    border: `1px solid ${active ? COLORS.plum : "rgba(123,47,89,.18)"}`,
    background: active ? COLORS.roseDim : "rgba(255,255,255,.88)",
    color: disabled ? COLORS.textFaint : active ? COLORS.plum : COLORS.text,
    borderRadius: 13,
    minHeight: 40,
    padding: "7px 10px",
    fontSize: 11,
    fontWeight: 900,
    cursor: disabled ? "not-allowed" : "pointer",
    opacity: disabled ? 0.55 : 1,
    boxShadow: active
      ? "0 14px 30px rgba(123,47,89,.14)"
      : "0 10px 24px rgba(60,20,50,.06)",
    transition: LUXURY_TRANSITION,
  }),
  templateToolbar: {
    display: "flex",
    alignItems: "center",
    gap: 5,
    padding: "5px 8px",
    borderBottom: "1px solid rgba(123,47,89,.12)",
    background: "rgba(255,255,255,.9)",
    flexWrap: "nowrap",
    overflowX: "auto",
    overflowY: "hidden",
    whiteSpace: "nowrap",
    position: "sticky",
    top: 0,
    zIndex: 4,
  },
  templateToolbarTitle: {
    color: COLORS.plum,
    fontSize: 12,
    fontWeight: 900,
    letterSpacing: ".12em",
    textTransform: "uppercase",
    marginRight: 4,
  },
  templateToolbarButton: (active = false, disabled = false) => ({
    border: `1px solid ${active ? COLORS.plum : "rgba(123,47,89,.16)"}`,
    background: active ? COLORS.roseDim : "#fff",
    color: disabled ? COLORS.textFaint : active ? COLORS.plum : COLORS.text,
    borderRadius: 999,
    minHeight: 30,
    padding: "5px 8px",
    fontSize: 11,
    fontWeight: 800,
    cursor: disabled ? "not-allowed" : "pointer",
    opacity: disabled ? 0.55 : 1,
    transition: LUXURY_TRANSITION,
  }),
  zoomPill: {
    display: "flex",
    alignItems: "center",
    gap: 5,
    minHeight: 40,
    padding: "4px 6px",
    borderRadius: 14,
    border: "1px solid rgba(123,47,89,.12)",
    background: "#fff",
    color: COLORS.plum,
    fontWeight: 900,
    boxShadow: "0 10px 24px rgba(60,20,50,.06)",
  },
  zoomButton: {
    width: 32,
    height: 30,
    border: 0,
    borderRadius: 10,
    background: COLORS.roseDim,
    color: COLORS.plum,
    fontSize: 14,
    fontWeight: 900,
    cursor: "pointer",
    transition: LUXURY_TRANSITION,
  },
  canvasModeButton: {
    minHeight: 42,
    border: "1px solid rgba(59,31,53,.32)",
    borderRadius: 14,
    padding: "7px 10px",
    color: "#fff",
    background: "linear-gradient(135deg, #3B1F35, #7B2F59)",
    fontSize: 13,
    fontWeight: 900,
    letterSpacing: ".1em",
    textTransform: "uppercase",
    cursor: "pointer",
    boxShadow: "0 10px 22px rgba(60,20,50,.18)",
    transition: LUXURY_TRANSITION,
  },
  studioBar: {
    display: "grid",
    gridTemplateColumns: "repeat(6, minmax(104px, 1fr))",
    gap: 6,
    padding: "6px clamp(10px, 1.3vw, 14px)",
    borderBottom: "1px solid rgba(123,47,89,.12)",
    background: "linear-gradient(90deg, #fff, #fff7fb)",
    overflowX: "auto",
    overflowY: "hidden",
    boxSizing: "border-box",
  },
  layout: {
    flex: 1,
    minHeight: 0,
    height: "min(100%, calc(100vh - 154px))",
    display: "grid",
    gridTemplateColumns:
      "minmax(240px, 0.74fr) minmax(520px, 2.15fr) minmax(210px, 0.58fr)",
    gap: 10,
    padding: "5px clamp(6px, .8vw, 10px)",
    overflowX: "hidden",
    overflow: "hidden",
    alignItems: "stretch",
    background: ANITASET_MARBLE_BACKGROUND,
    backgroundAttachment: "local",
  },
  focusLayout: {
    flex: 1,
    minHeight: 0,
    display: "grid",
    gridTemplateColumns: "minmax(0, 1fr)",
    padding: "8px clamp(8px, 1vw, 12px)",
    overflow: "hidden",
    background: ANITASET_MARBLE_BACKGROUND,
    backgroundAttachment: "local",
  },
  panel: {
    background: COLORS.surface,
    border: `1px solid ${COLORS.border}`,
    borderRadius: 17,
    boxShadow: "0 18px 46px rgba(60,20,50,.08)",
    transition: "box-shadow 220ms ease, transform 220ms ease",
    minHeight: 0,
    minWidth: 0,
    overflow: "auto",
    maxHeight: "calc(100vh - 168px)",
  },
  activeStudioPanel: {
    display: "flex",
    flexDirection: "column",
    overflow: "hidden",
    maxHeight: "calc(100vh - 168px)",
    minWidth: 0,
    position: "relative",
    zIndex: 1,
  },
  activeStudioHeader: {
    flex: "0 0 auto",
    padding: "8px 11px 6px",
    borderBottom: "1px solid rgba(123,47,89,.12)",
    color: COLORS.plum,
    fontSize: 12,
    fontWeight: 900,
    letterSpacing: ".12em",
    textTransform: "uppercase",
    display: "grid",
    gap: 4,
  },
  activeStudioScroll: {
    flex: "1 1 auto",
    minHeight: 0,
    overflowY: "auto",
    overflowX: "hidden",
    overscrollBehavior: "contain",
    padding: 10,
  },
  stickyPreview: {
    position: "relative",
    zIndex: 2,
    background: ANITASET_MARBLE_BACKGROUND,
    flex: "1 1 auto",
    minHeight: "clamp(520px, calc(100vh - 300px), 820px)",
    padding: "0",
    display: "grid",
    placeItems: "stretch",
    overflow: "hidden",
  },
  previewFrame: {
    width: "100%",
    height: "100%",
    minHeight: 0,
  },
  panelPad: { padding: 8 },
  nailStackPanel: {
    overflow: "hidden",
    maxHeight: "calc(100vh - 168px)",
  },
  nailStackPad: {
    padding: "10px 10px 8px",
    display: "grid",
    gap: 8,
    alignContent: "start",
  },
  sectionTitle: {
    fontSize: 11,
    letterSpacing: ".08em",
    textTransform: "uppercase",
    color: COLORS.textMuted,
    fontWeight: 800,
    marginBottom: 3,
  },
  miniButton: (active = false) => ({
    ...(active ? S.btnSecondary : S.btnGhost),
    padding: "8px 10px",
    fontSize: 12,
    borderColor: active ? COLORS.plum : COLORS.border,
    color: active ? COLORS.plum : COLORS.textMuted,
    whiteSpace: "nowrap",
  }),
  panelSection: {
    border: `1px solid ${COLORS.border}`,
    borderRadius: 12,
    marginBottom: 6,
    background: "#fff",
    overflow: "hidden",
    boxShadow: "0 10px 24px rgba(60,20,50,.05)",
    transition: "box-shadow 180ms ease, border-color 180ms ease",
  },
  panelHeader: {
    width: "100%",
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: 7,
    padding: "6px 8px",
    border: 0,
    borderBottom: `1px solid ${COLORS.border}`,
    background: COLORS.roseDim,
    cursor: "pointer",
    transition: LUXURY_TRANSITION,
  },
  panelBody: { padding: 8, animation: "anitasetDrawerSettle 220ms cubic-bezier(.2,.8,.2,1)" },
  actionGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(4, minmax(42px, 1fr))",
    gap: 6,
    alignItems: "stretch",
  },
  iconOnlyButton: (active = false, disabled = false) => ({
    border: `1px solid ${active ? COLORS.plum : COLORS.border}`,
    background: active ? COLORS.roseDim : "#fff",
    color: disabled ? COLORS.textFaint : active ? COLORS.plum : COLORS.text,
    borderRadius: 10,
    padding: "7px 8px",
    minHeight: 34,
    fontSize: 15,
    fontWeight: 800,
    cursor: disabled ? "not-allowed" : "pointer",
    opacity: disabled ? 0.55 : 1,
    transition: LUXURY_TRANSITION,
  }),
  iconButton: (active = false, disabled = false) => ({
    border: `1px solid ${active ? COLORS.plum : COLORS.border}`,
    background: active ? COLORS.roseDim : "#fff",
    color: disabled ? COLORS.textFaint : active ? COLORS.plum : COLORS.text,
    borderRadius: 10,
    padding: "8px 10px",
    fontSize: 12,
    fontWeight: 700,
    cursor: disabled ? "not-allowed" : "pointer",
    opacity: disabled ? 0.55 : 1,
    transition: LUXURY_TRANSITION,
  }),
  field: { marginBottom: 12 },
  row: { display: "flex", gap: 7, alignItems: "center" },
  smallText: { fontSize: 12, color: COLORS.textMuted, lineHeight: 1.35 },
  techniqueChoiceGrid: {
    display: "grid",
    gridTemplateColumns: "repeat(2, minmax(0, 1fr))",
    gap: 7,
    marginBottom: 12,
  },
  techniqueChoiceButton: (active = false) => ({
    ...S.btnSecondary,
    minHeight: 74,
    padding: "12px 10px",
    borderRadius: 18,
    borderColor: active ? COLORS.plum : "rgba(123,47,89,.18)",
    background: active ? COLORS.roseDim : "#fff",
    color: active ? COLORS.plum : COLORS.text,
    fontWeight: 900,
    boxShadow: active
      ? "0 0 0 3px rgba(255,214,232,.82), 0 14px 28px rgba(123,47,89,.14)"
      : "0 10px 22px rgba(60,20,50,.06)",
  }),
  studioCard: (active = false) => ({
    display: "grid",
    gridTemplateColumns: "26px 1fr",
    gap: 6,
    alignItems: "center",
    width: "100%",
    minHeight: 40,
    padding: "5px 8px",
    border: `1px solid ${active ? COLORS.plum : "rgba(123,47,89,.16)"}`,
    borderRadius: 18,
    background: active
      ? "linear-gradient(135deg, #fff0f8, #fff)"
      : "linear-gradient(135deg, #fff, #fff8fb)",
    color: COLORS.text,
    textAlign: "left",
    cursor: "pointer",
    boxShadow: active
      ? "0 0 0 2px rgba(255,214,232,.88), 0 14px 30px rgba(123,47,89,.14)"
      : "0 8px 18px rgba(60,20,50,.055)",
    transform: active ? "translateY(-1px) scale(1.01)" : "translateY(0) scale(1)",
    transition: LUXURY_TRANSITION,
  }),
  studioCardIcon: {
    width: 26,
    height: 26,
    borderRadius: 9,
    display: "grid",
    placeItems: "center",
    background: "linear-gradient(135deg, #3B1F35, #7B2F59)",
    color: "#fff",
    fontSize: 14,
    boxShadow: "0 8px 18px rgba(60,20,50,.15)",
    transition: LUXURY_TRANSITION,
  },
  studioCardTitle: {
    display: "block",
    fontSize: 12,
    fontWeight: 900,
    letterSpacing: ".08em",
    textTransform: "uppercase",
    color: COLORS.plum,
  },
  studioCardCopy: {
    display: "none",
    marginTop: 2,
    fontSize: 10,
    lineHeight: 1.18,
    color: COLORS.textMuted,
  },
  focusPreviewPanel: {
    maxHeight: "calc(100vh - 154px)",
  },
  focusEdgeTabs: {
    position: "fixed",
    left: 8,
    top: "42%",
    zIndex: 1300,
    display: "grid",
    gap: 6,
  },
  edgeTab: {
    writingMode: "vertical-rl",
    transform: "rotate(180deg)",
    border: "1px solid rgba(123,47,89,.18)",
    background: "#fff",
    color: COLORS.plum,
    borderRadius: 999,
    padding: "10px 6px",
    fontSize: 11,
    fontWeight: 900,
    letterSpacing: ".08em",
    textTransform: "uppercase",
    boxShadow: "0 12px 28px rgba(60,20,50,.12)",
    cursor: "pointer",
  },
  heroCanvasPanel: {
    display: "flex",
    flexDirection: "column",
    alignItems: "stretch",
    overflow: "visible",
    minWidth: 0,
    border: "1px solid rgba(123,47,89,.12)",
    background: ANITASET_MARBLE_BACKGROUND,
    transition: "box-shadow 220ms ease, border-color 220ms ease",
  },
  heroCanvasHeader: {
    padding: "6px 12px 0",
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: 12,
    color: COLORS.plum,
    fontWeight: 900,
    letterSpacing: ".14em",
    textTransform: "uppercase",
  },
  nailStackHeader: {
    padding: "2px 1px 7px",
    borderBottom: "1px solid rgba(123,47,89,.08)",
    marginBottom: 3,
  },
  historyPlaceholder: {
    marginTop: 12,
    padding: 10,
    borderRadius: 14,
    border: "1px dashed rgba(123,47,89,.26)",
    background: "rgba(255,247,251,.75)",
  },
  compactAssetGridDensity: {
    cardPadding: 4,
    gridGap: 6,
    tileMinHeight: 58,
    iconSize: 50,
    minTouchTarget: 44,
  },
  studioDock: {
    display: "flex",
    gap: 7,
    padding: "8px 14px 10px",
    borderTop: "1px solid rgba(123,47,89,.14)",
    background: "linear-gradient(90deg, #fffaf7, #fff4fb)",
    overflowX: "auto",
    flexShrink: 0,
    position: "relative",
    zIndex: 2,
  },
  dockButton: (active = false) => ({
    minHeight: 44,
    padding: "8px 14px",
    borderRadius: 16,
    border: `1px solid ${active ? COLORS.plum : "rgba(123,47,89,.16)"}`,
    background: active ? COLORS.roseDim : "#fff",
    color: active ? COLORS.plum : COLORS.textMuted,
    fontWeight: 900,
    whiteSpace: "nowrap",
    cursor: "pointer",
    transition: LUXURY_TRANSITION,
  }),
};
